interface:
  display_name: "Normalize Brand"
  short_description: "Clean up or clarify messy tenant brand materials."
  default_prompt: "Use $normalize-brand when this tenant folder is messy or ambiguous and needs a reusable normalized brand profile plus validation report."
